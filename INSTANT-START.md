# 🚀 CCBoss - Start in 1 Second

## ONE COMMAND:

```bash
cd ~/CC/ccboss && ./SUPER-EASY-START.sh
```

**That's it!** CCBoss is now working with your SkillBoss API key.

## Try it:

```bash
cb ask "Hello!"
cb ask "解释AI"
cb                    # Start interactive chat
```

## All shortcuts work:
- `cb` - 2 characters (fastest!)
- `ccb` - 3 characters
- `ccboss` - full command
- `boss` - cool command

## Need different API?

```bash
cb setup    # Configuration wizard for any provider
```

Supports: SkillBoss, Kimi, OpenAI, Claude, DeepSeek, and 10+ more!

---

**That's literally it. One command, done. 🎉**
